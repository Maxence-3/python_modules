[tool.poetry]
name        = "matrix-loading"
version     = "0.1.0"
description = "Chapter V – Exercise 01: Loading Programs (Matrix data analysis)"
authors     = ["student <student@42.fr>"]
readme      = "README.md"

[tool.poetry.dependencies]
python      = "^3.10"
pandas      = "^2.1.0"
numpy       = "^1.25.0"
matplotlib  = "^3.7.0"
requests    = "^2.31.0"

[tool.poetry.scripts]
loading = "loading:main"

[build-system]
requires      = ["poetry-core"]
build-backend = "poetry.core.masonry.api"